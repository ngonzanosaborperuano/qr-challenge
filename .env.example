# Variables de entorno para Docker Compose
# Copia este archivo a .env y ajusta los valores según necesites
# Docker Compose automáticamente carga este archivo si existe en la raíz del proyecto

# ============================================
# API Node.js
# ============================================
NODE_API_PORT=3001
NODE_ENV=development

# ============================================
# API Go
# ============================================
GO_API_PORT=3000
NODE_API_URL=http://node-api:3001

# ============================================
# Desarrollo (usado en docker-compose.dev.yml)
# ============================================
# CGO_ENABLED=0

# Nota: Los valores después de :- son los valores por defecto si la variable no está definida
# Ejemplo: ${NODE_API_PORT:-3001} usa 3001 si NODE_API_PORT no está definida

# ============================================
# JWT Secret (para ambas APIs)
# ============================================
JWT_SECRET=your-super-secret-jwt-key-change-in
